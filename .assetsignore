# Files that live in this folder but must NOT be published.
#
# WHY (2026-09-09): `wrangler pages deploy .` uploads everything in the directory, .gitignore
# and all — so the three draft front pages and the old front-page backup were live on
# somosincontrol.com. Chris found out from an audit: "why does it show the old page. I don't
# want that of course."
preview/
*.bak

# Dead files kept in git but NOT published (2026-09-09 audit, F12):
#   the three PDFs carry pre-v1.1 content and are unlinked but were still reachable;
#   counts.json is the retired static poll tally, replaced by the live Worker /results.
downloads/
somos-incontrol/counts.json
